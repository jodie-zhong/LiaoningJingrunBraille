define(function (require, exports, module) {
    "use strict";

    var global = require('./global');

    var PageList = function () {

        /***
         * 初始化
         */
        this.init = function () {
            // 暂时写死一个页面
            global.pagesData.push({
                page: 'page1',
                pageName: '测试',
                widget:[]
            });
        };
    };

    module.exports = new PageList();

});